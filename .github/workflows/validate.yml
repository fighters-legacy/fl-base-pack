name: Validate Assets

on:
  pull_request:
    branches: [main]

jobs:
  license-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: fsfe/reuse-action@v4

  asset-naming:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check asset naming conventions
        shell: bash
        run: |
          set -euo pipefail
          FAIL=0

          # No spaces in filenames
          if find aircraft entities sensors weapons terrain missions audio ai -name "* *" 2>/dev/null | grep -q .; then
            echo "ERROR: filenames with spaces:"
            find aircraft entities sensors weapons terrain missions audio ai -name "* *"
            FAIL=1
          fi

          # Asset filenames must be lowercase (Linux ext4 is case-sensitive, and the engine
          # lowercases asset names before resolving them, so an uppercase asset silently misses).
          #
          # DOCUMENTATION IS NOT AN ASSET. `SOURCES.md` is REQUIRED in every aircraft directory by
          # docs/legal/aircraft-likeness.md — it is the provenance manifest that makes the whole
          # policy auditable — and it is uppercase by the same convention as README.md and LICENSE.
          # The engine never loads it. Policing its case would fail every conforming aircraft PR.
          if find aircraft entities sensors weapons terrain missions audio ai -name "*[A-Z]*" ! -name "*.md" 2>/dev/null | grep -q .; then
            echo "ERROR: uppercase asset filenames (Linux ext4 is case-sensitive):"
            find aircraft entities sensors weapons terrain missions audio ai -name "*[A-Z]*" ! -name "*.md"
            FAIL=1
          fi

          # No Windows reserved name stems (cause hangs/errors on Windows)
          while IFS= read -r f; do
            stem=$(basename "$f" | sed 's/\.[^.]*$//')
            if echo "$stem" | grep -iqE '^(con|nul|prn|aux|com[1-9]|lpt[1-9])$'; then
              echo "ERROR: Windows reserved filename: $f"
              FAIL=1
            fi
          done < <(find aircraft entities sensors weapons terrain missions audio ai -type f 2>/dev/null)

          # OGG files only under audio/
          if find . -name "*.ogg" -not -path "./audio/*" 2>/dev/null | grep -q .; then
            echo "ERROR: .ogg files outside audio/:"
            find . -name "*.ogg" -not -path "./audio/*"
            FAIL=1
          fi

          # Lua files only under ai/
          if find . -name "*.lua" -not -path "./ai/*" 2>/dev/null | grep -q .; then
            echo "ERROR: .lua files outside ai/:"
            find . -name "*.lua" -not -path "./ai/*"
            FAIL=1
          fi

          # Backslash path separators in TOML (Windows paths appear as \\ in source)
          if find aircraft terrain missions -name "*.toml" 2>/dev/null | \
              xargs grep -lP '\\\\[A-Za-z0-9_-]' 2>/dev/null | grep -q .; then
            echo "ERROR: backslash path separators in TOML (use forward slashes):"
            find aircraft terrain missions -name "*.toml" | \
              xargs grep -nP '\\\\[A-Za-z0-9_-]'
            FAIL=1
          fi

          # Backslash path separators in YAML
          if find missions -name "*.yaml" 2>/dev/null | \
              xargs grep -lP '\\[A-Za-z0-9_-]' 2>/dev/null | grep -q .; then
            echo "ERROR: backslash path separators in YAML (use forward slashes):"
            find missions -name "*.yaml" | \
              xargs grep -nP '\\[A-Za-z0-9_-]'
            FAIL=1
          fi

          # Mesh directory layout. Two checks, and the split matters:
          #
          #   1. EVERY mesh directory must have an entity def. An `aircraft/<name>/<name>.glb` that
          #      no `entities/<name>.toml` claims is a mesh nothing can ever spawn -- dead weight in
          #      the mod archive. validate-entity cannot catch this: it walks entity defs and
          #      resolves their assets, so a mesh no def mentions is invisible to it.
          #
          #   2. Only an AIR VEHICLE must also carry a flight model beside its mesh. The original
          #      rule demanded `<name>.toml` of every mesh directory, which was right while this
          #      pack held nothing but aircraft and became wrong the moment it grew ground units
          #      (#17): a SAM launcher, a gun vehicle, a fuel depot and a truck have NO flight model
          #      by design -- an empty `flight_model` is what tells the engine not to integrate them,
          #      and they sit where a mission's `start: ground` puts them.
          #
          # ⚠ Note both checks key on the DIRECTORY NAME matching the entity def's file name, which
          # is this pack's convention throughout. If that ever stops being true, these become
          # useless rather than wrong -- they will simply stop firing.
          for dir in aircraft/*/; do
            [ -d "$dir" ] || continue
            name=$(basename "$dir")
            [ -f "${dir}${name}.glb" ] || continue
            if [ ! -f "entities/${name}.toml" ]; then
              echo "ERROR: ${dir}${name}.glb has no entities/${name}.toml -- nothing can spawn it"
              FAIL=1
              continue
            fi
            if grep -qE '^[[:space:]]*category[[:space:]]*=[[:space:]]*"air_vehicle"' \
                "entities/${name}.toml" && [ ! -f "${dir}${name}.toml" ]; then
              echo "ERROR: ${dir}${name}.glb is an air_vehicle mesh with no ${name}.toml flight model"
              FAIL=1
            fi
          done

          [ $FAIL -eq 0 ] && echo "All naming checks passed."
          exit $FAIL

  # Radio voice lines (#13). The engine has no validator for these -- the binding is the filename, so
  # a missing or malformed OGG is not an error anywhere, it is just a line that silently stays a
  # subtitle. This leg is the only thing that would notice.
  #
  # It runs the generator's --check-assets mode, which needs ffmpeg and NOTHING ELSE: no piper, no
  # 300 MB of voice models, no GPU. The two heavier modes (--check re-synthesises; --verify-speech
  # transcribes the audio back and compares the words) are author-side, like the mesh and SFX
  # generators -- the OGGs are committed artifacts and CI does not rebuild them.
  voice-lines-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install ffmpeg + numpy
        # ffmpeg is NOT preinstalled on ubuntu-latest (the first run of this leg failed on a missing
        # ffprobe). The check needs ffprobe for the container/format assertions and ffmpeg to decode
        # for the silence test; both come from the one package.
        run: |
          sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends ffmpeg
          python -m pip install numpy
      - name: Check voice-line coverage and format
        run: python tools/voicegen/gen_voice_lines.py --check-assets

  # Music + playlist (#5). Two checks that catch different failures, both cheap:
  #
  #   validate-playlist   the engine's own parser -- state ids, track asset names, crossfade.
  #   --check-assets      BOTH-WAYS coverage between data/playlist.toml and audio/music/, plus
  #                       format and edge-silence. A track nobody references never plays; a
  #                       reference with no track sends that game state silent. Neither is an error
  #                       anywhere else, which is exactly why they would ship.
  #
  # Generation is author-side (a 10 GB model and a GPU); the OGGs are committed artifacts.
  music-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Install ffmpeg + numpy
        run: |
          sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends ffmpeg
          python -m pip install numpy
      - name: Fetch validator
        uses: ./.github/actions/engine-validators
        with:
          require: validate-playlist
      - name: Validate playlist
        run: validate-playlist --playlist data/playlist.toml --pack .
      - name: Check track coverage and format
        run: python tools/musicgen/gen_music.py --check-assets

  # ── Training syllabus (#15) ────────────────────────────────────────────────────────────────────
  # The FIRST leg in this pack that runs the ENGINE rather than a validator, and it exists because
  # the validators cannot see the failure mode that matters here. A lesson is a Lua script; a script
  # that dies at load leaves its entity with no controller, and the mission then loads cleanly,
  # spawns everything, and does nothing for the rest of its life. `validate-mission` calls that
  # green, because the file is perfectly well formed.
  #
  # Three real failures were caught this way while the syllabus was being written, including every
  # lesson script failing at `require()` (engine #1210). None was visible to any other check.
  #
  # It is cheap: six headless missions plus three predicate fixtures, a couple of seconds of sim
  # each, no GPU, no network beyond the engine release the other legs already fetch. fl-server sits
  # at the release archive root (`install(TARGETS fl-server RUNTIME DESTINATION .)`), so the same
  # action that fetches validators fetches it.
  training-missions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Fetch fl-server
        uses: ./.github/actions/engine-validators
        with:
          require: fl-server
      - name: Probe the training syllabus
        run: python3 tools/missiontest/probe_training.py

  strike-missions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Fetch fl-server
        uses: ./.github/actions/engine-validators
        with:
          require: fl-server
      - name: Probe the strike and instant-action set
        run: python3 tools/missiontest/probe_strike.py

  flight-model-validate:
    runs-on: ubuntu-latest
    # HARD GATE (#7). validate-flight-model (post-#823) knows that `aero.drag_polar.k = 0.0` is
    # REQUIRED of an aircraft that authors a `cd_table` (the table is TOTAL clean drag; a non-zero
    # k would double-count the induced term). The fetch below pulls the latest engine release, so
    # this job gates for real and sits in the branch protection required checks alongside
    # mission-validate.
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Find changed flight models
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # A `*.expect.toml` sits beside the flight model it describes and states the performance
          # that model must reproduce. It is a test fixture, not a flight model: it has no
          # [flight_model] table and the validator rejects it on sight. The obvious
          # `aircraft/**/*.toml` glob sweeps it in and would fail every aircraft PR on a file that
          # is doing its job.
          #
          # The `[ -f ]` filter drops files this PR DELETED. `git diff --name-only` lists a deletion
          # exactly like an edit, but the path is not in the checkout, so passing it on would fail
          # the leg on a file whose removal is the change. Every changed-file leg needs this.
          FILES=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- aircraft \
            | { grep -E '\.toml$' || true; } \
            | { grep -v -E '\.expect\.toml$' || true; } \
            | while IFS= read -r f; do if [ -f "$f" ]; then echo "$f"; fi; done \
            | tr '\n' ' ')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"
          echo "Flight models to validate: ${FILES:-(none)}"
      - name: Fetch validators
        if: steps.changed.outputs.files != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-flight-model
      - name: Run flight model validator
        if: steps.changed.outputs.files != ''
        run: validate-flight-model ${{ steps.changed.outputs.files }}

  # fm-trim --expect: the Phase-4 gate from #1/#54, mechanised. Where validate-flight-model checks a
  # model is well-formed, fm-trim derives its performance and fails when it stops reproducing the
  # numbers its flight manual publishes (stall speed, max Mach, ROC, sustained turn, fuel burn). It
  # runs each changed model against its sibling `<name>.expect.toml`. Every engine release since
  # v0.3.10 ships fm-trim, so this is a hard gate: a release that stopped shipping it would be a
  # packaging regression worth failing on, not skipping past.
  fm-trim-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Find changed flight models
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # Same glob as flight-model-validate: aircraft TOMLs, minus the *.expect.toml fixtures
          # (which have no [flight_model] table and are the expectation input, not a model).
          #
          # The `[ -f ]` filter drops files this PR DELETED. `git diff --name-only` lists a deletion
          # exactly like an edit, but the path is not in the checkout, so passing it on would fail
          # the leg on a file whose removal is the change. Every changed-file leg needs this.
          FILES=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- aircraft \
            | { grep -E '\.toml$' || true; } \
            | { grep -v -E '\.expect\.toml$' || true; } \
            | while IFS= read -r f; do if [ -f "$f" ]; then echo "$f"; fi; done \
            | tr '\n' ' ')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"
          echo "Flight models to fm-trim: ${FILES:-(none)}"
      - name: Fetch fm-trim
        if: steps.changed.outputs.files != ''
        uses: ./.github/actions/engine-validators
        with:
          require: fm-trim
      - name: Run fm-trim --expect
        if: steps.changed.outputs.files != ''
        shell: bash
        run: |
          set -euo pipefail
          rc=0
          for model in ${{ steps.changed.outputs.files }}; do
            expect="${model%.toml}.expect.toml"
            if [ -f "$expect" ]; then
              echo "fm-trim $model --expect $expect"
              fm-trim "$model" --expect "$expect" || rc=1
            else
              echo "note: $model has no $expect — skipping its performance check"
            fi
          done
          exit $rc

  mesh-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Find changed meshes
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # `<id>_cockpit.glb` is EXCLUDED, and that is not a convenience -- it is a real conflict
          # between three sources of truth, recorded here so nobody "fixes" it by deleting this
          # filter:
          #
          #   - docs/modding/3d-models.md says a cockpit file carries `camera_anchor` AND the
          #     instrument panel geometry;
          #   - the engine renders no cockpit interior at all today, so ours is a camera-anchor
          #     empty and nothing else;
          #   - validate-mesh rejects any file with no meshes ("no meshes found"), so it rejects
          #     the very file the engine's own convention asks us to ship.
          #
          # Filed as fighters-legacy#844. When the validator learns the camera-anchor convention,
          # drop this filter.
          #
          # LOD siblings are not listed either: validate-mesh discovers and validates them itself
          # from the base mesh, so naming them would only validate them twice.
          #
          # `[ -f ]`: a mesh this PR DELETED is listed by git diff but is not in the checkout.
          FILES=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- aircraft \
            | { grep -E '\.glb$' || true; } \
            | { grep -v -E '_cockpit\.glb$' || true; } \
            | { grep -v -E '_lod[0-9]+\.glb$' || true; } \
            | while IFS= read -r f; do if [ -f "$f" ]; then echo "$f"; fi; done \
            | tr '\n' ' ')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"
          echo "Meshes to validate: ${FILES:-(none)}"
      - name: Fetch validators
        if: steps.changed.outputs.files != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-mesh
      - name: Run mesh validator
        if: steps.changed.outputs.files != ''
        run: validate-mesh ${{ steps.changed.outputs.files }}

  mission-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Find changed missions
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # `[ -f ]`: a mission this PR DELETED is listed by git diff but is not in the checkout.
          # `campaign_*` is EXCLUDED on purpose: a campaign file lives in missions/ by the engine's
          # contract but is a campaign graph, not a mission — validate-mission would reject it for
          # missing objects/triggers. It has its own campaign-validate leg. Story missions and
          # everything else in missions/ stay in this glob; templates/ never enter it.
          FILES=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- missions \
            | { grep -E '\.ya?ml$' || true; } \
            | { grep -vE '^missions/campaign_' || true; } \
            | while IFS= read -r f; do if [ -f "$f" ]; then echo "$f"; fi; done \
            | tr '\n' ' ')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"
          echo "Missions to validate: ${FILES:-(none)}"
      - name: Fetch validators
        if: steps.changed.outputs.files != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-mission
      - name: Run mission validator
        if: steps.changed.outputs.files != ''
        # --pack cross-checks each mission object's crew: block against the referenced
        # entity's declared [[crew]] seats -- load-bearing once Stage-4 missions carry crew.
        run: validate-mission --pack . ${{ steps.changed.outputs.files }}

  # Campaign graph + theaters + frontline rasters + template resolution (#14). HARD GATE, no
  # `optional:` hatch: validate-campaign ships in every release since v0.3.10. `--pack .` makes it
  # resolve every theater id to a manifest with valid bounds, every story/template file, and every
  # frontline PNG (8-bit grayscale, dims == frontline_grid).
  campaign-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Detect campaign content changes
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          CHANGED=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" \
            -- 'missions/campaign_*' theaters frontlines templates | tr '\n' ' ')
          echo "changed=$CHANGED" >> "$GITHUB_OUTPUT"
          echo "Campaign content changes: ${CHANGED:-(none)}"
      - name: Fetch validator
        if: steps.changed.outputs.changed != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-campaign
      - name: Run campaign validator
        if: steps.changed.outputs.changed != ''
        run: |
          set -euo pipefail
          for c in missions/campaign_*.yaml; do
            validate-campaign --pack . "$c"
          done

  # ── Weapons / sensors / entity legs (engine #583). ─────────────────────────────────────────────
  # HARD GATES. These legs were born `optional: true` because the engine release of the day
  # (v0.3.1) predated the #583 validators; every release since v0.3.10 ships the full validator
  # set, so the auto-activation hatch is retired -- a release missing a binary now fails the leg
  # as the packaging regression it would be, instead of skipping green past it.
  #
  # validate-entity/validate-weapon run in `--pack` mode (they cross-check references across the
  # whole pack: hardpoint weapon ids, seeker sensor_id, every asset name), so they take the pack
  # root, not a changed-file list. A change anywhere in the pack's asset dirs triggers a re-run.
  entity-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Detect pack asset changes
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # validate-entity --pack resolves mesh/cockpit/flight_model/ai_script/damage_mesh/manual
          # asset names AND sensor/weapon def ids, so any of these dirs changing warrants a re-run.
          CHANGED=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" \
            -- entities sensors weapons aircraft ai | tr '\n' ' ')
          echo "changed=$CHANGED" >> "$GITHUB_OUTPUT"
          echo "Pack asset changes: ${CHANGED:-(none)}"
      - name: Fetch validator
        if: steps.changed.outputs.changed != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-entity
      - name: Run entity validator (whole pack)
        if: steps.changed.outputs.changed != ''
        run: validate-entity --pack .

  weapon-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Detect weapon/sensor changes
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # --pack cross-checks each seeker.sensor_id against the pack's sensor defs, so a sensor
          # change can invalidate a weapon and vice versa: watch both dirs.
          CHANGED=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" \
            -- weapons sensors | tr '\n' ' ')
          echo "changed=$CHANGED" >> "$GITHUB_OUTPUT"
          echo "Weapon/sensor changes: ${CHANGED:-(none)}"
      - name: Fetch validator
        if: steps.changed.outputs.changed != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-weapon
      - name: Run weapon validator (whole pack)
        if: steps.changed.outputs.changed != ''
        run: validate-weapon --pack .

  sensor-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Find changed sensors
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          # validate-sensor has no --pack mode; it validates each file, so pass the changed list.
          #
          # `[ -f ]`: a sensor def this PR DELETED is listed by git diff but is not in the checkout.
          # This is not hypothetical -- retiring `eyeball_night` is the change that found it. The
          # references a deletion breaks are caught by entity-validate's whole-pack `--pack` run,
          # which is the right leg for that: it resolves every `sensors = [...]` id in the pack.
          FILES=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- sensors \
            | { grep -E '\.toml$' || true; } \
            | while IFS= read -r f; do if [ -f "$f" ]; then echo "$f"; fi; done \
            | tr '\n' ' ')
          echo "files=$FILES" >> "$GITHUB_OUTPUT"
          echo "Sensors to validate: ${FILES:-(none)}"
      - name: Fetch validator
        if: steps.changed.outputs.files != ''
        uses: ./.github/actions/engine-validators
        with:
          require: validate-sensor
      - name: Run sensor validator
        if: steps.changed.outputs.files != ''
        run: validate-sensor ${{ steps.changed.outputs.files }}
